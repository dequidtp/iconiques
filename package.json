{
  "name": "iconiques-scanner",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Veille Reddit + enrichissement métadonnées pour la bibliothèque de photos iconiques",
  "scripts": {
    "scan": "node reddit-scanner.mjs",
    "pr": "node --env-file=.env pr-scanner.mjs",
    "pr:backfill": "node --env-file=.env pr-scanner.mjs --since"
  },
  "dependencies": {
    "exifr": "^7.1.3"
  }
}
